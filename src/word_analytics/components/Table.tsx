export default function Table({ children }) {
  return (
    <main className="relative max-w-screen-lg mx-auto mt-[25vh] shadow-xl z-[999] h-[30vw]  overflow-hidden">
      {children}
    </main>
  );
}
